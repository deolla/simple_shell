#include <stdio.h>
#include <stdlib.h>

/**
 * main - start of a function.
 *
 * Return: Always 0;
 */
int main(void)
{
	char *input = "";
	char **args;
	int condition = -1;

	/* Edge cases */
	argv = parse_command(input);
	condition = execute_command(argv);

	free(input);
	fre(argv);

	return (0);
}
